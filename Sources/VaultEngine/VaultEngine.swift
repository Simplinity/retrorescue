/// VaultEngine — Create, open, and manage .retrovault bundles.
/// Preserves classic Mac file metadata (resource forks, type/creator codes,
/// Finder flags, dates) on modern filesystems.
public enum VaultEngine {
    public static let version = "0.1.0"
}
